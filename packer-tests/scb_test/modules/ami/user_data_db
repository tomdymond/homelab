#cloud-config
repo_update: true
repo_upgrade: all

packages:
  - mariadb-server

runcmd:
  - service mariadb start
  - mysqladmin --no-defaults --port=3308 --user=root password '${database_password}'

